# Mode 1 Studio redesign

Visual refresh for the ESP32-S3-Matrix / QMI8658 BOOT-delimited NoodleAI workflow.

The redesign deliberately preserves the existing application IDs and BLE/training behavior. It adds:

- Mode 1 / ESP32-S3-Matrix identity
- visible BOOT-gate status
- BOOT workflow diagram
- dedicated BOOT capture card
- polished on-device prediction surface
- responsive dark-shell / light-workspace layout
- cache-busted local assets

The neural/data behavior remains the existing Mode 1 behavior.
